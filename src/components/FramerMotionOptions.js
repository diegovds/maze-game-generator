const initial = { opacity: 0, scale: 0.5 };

const whileInView = { opacity: 1, scale: 1 };

const transition = { duration: 0.8, delay: 0.25, ease: [0, 0.71, 0.2, 1.01] };

export { initial, whileInView, transition };
